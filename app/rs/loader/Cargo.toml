[package]
name = "loader"
version = "1.0.0"
edition = "2021"
description = "Open-source dynamic library loader - replacement for legacy libloader.so"
license = "MPL-2.0"

[lib]
name = "loader"
crate-type = ["cdylib"]

[dependencies]
